# 常规文本文件统一使用 LF，便于跨平台协作。
*.md text eol=lf
*.js text eol=lf
*.ts text eol=lf
*.json text eol=lf
*.mjs text eol=lf
*.html text eol=lf

# 原始导出必须保持逐字节不变，避免 Git 自动转换换行符。
/角色卡/原始导出/* -text

# 拆分出的内容参与文本 diff，但不做换行符归一化；原始内容里的尾随空格也必须保留。
/角色卡/工作区/Z5.20/** -text -whitespace
